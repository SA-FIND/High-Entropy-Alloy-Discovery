services:
  - type: web
    name: metaforge-web
    env: python
    region: oregon
    plan: free
    rootDir: MetaForge-Web
    buildCommand: "python -m pip install --upgrade pip && python -m pip install -r requirements.txt"
    startCommand: "python -m gunicorn app:app --bind 0.0.0.0:$PORT"
